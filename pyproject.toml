[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "molgnn-lab"
version = "0.1.0"
description = "Architecture-only framework for 2D molecular and 3D complex graph neural networks"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "astartes>=1.3,<2",
    "numpy>=2,<3",
    "pandas>=2.2,<4",
    "pyyaml>=6,<7",
    "rdkit>=2026.3,<2027",
    "scikit-learn>=1.6,<2",
    "torch>=2.9,<2.13",
    "torch-geometric>=2.7,<3",
]

[project.optional-dependencies]
# DimeNet constructs the fixed spherical-Bessel roots at model construction
# time.  Keeping SciPy optional preserves the lean import/runtime path for
# the existing 2D architectures.
dimenet = ["scipy>=1.14,<2"]
# GemNet uses the same construction-only root solver for its circular and
# full tensor Fourier--Bessel bases; forward passes remain pure PyTorch.
gemnet = ["scipy>=1.14,<2"]
# Equiformer requires spherical harmonics, irreps, and tensor products.  The
# narrow 0.5 range is deliberate: its public API was compatibility-tested
# against this project, whereas e3nn 0.6 is a potentially breaking 0.x minor.
equiformer = ["e3nn>=0.5.9,<0.6"]

[project.scripts]
molgnn = "molgnn.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/molgnn"]

[tool.hatch.build.targets.sdist]
only-include = ["src/molgnn"]
